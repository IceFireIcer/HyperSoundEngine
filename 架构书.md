# HyperSoundEngine v1 —— 架构书（Architecture Blueprint）

> **版本**：v1.0（2026-08，独立引擎重构后的正式架构）
> **代码**：`HyperSoundEngine/`（独立目录，不依赖任何特定宿主）
> **规模**：src 32 个 TS 文件（含接口/工厂/入口）+ ui 14 个 TSX/TS 文件；
> test 32 文件 / 331 用例全绿；`tsc --noEmit` 与 `tsc -p tsconfig.ui.json` 均 0 错误
> **读者**：维护者、接入方、代码审查者
> **配套文档**：`docs/API.md`（对外接口）｜`docs/ARCHITECTURE.md`（简要架构）｜
> `docs/INTEGRATION.md`（接入指南）｜`docs/FEATURES_VERIFICATION.md`（功能核验）｜
> `docs/代码构成与自研统计.md`（自研/现成统计）｜`src/dsp/API_SPEC.md`（DSP 模块契约）｜
> `THIRD_PARTY_NOTICES.md`（许可声明）

---

## 1. 定位与设计目标

HyperSoundEngine 是一个**独立软件 DSP 音频效果引擎**：纯 TypeScript 的 DSP 内核，
不依赖 Web Audio 节点、不依赖 React、不依赖任何特定宿主，可在任何 JS 运行时
（浏览器、AudioWorklet、Node、Electron 主/渲染进程）以同一份代码跑出**逐样本一致**的结果。

| 目标 | 具体含义 | 落地方式 |
|---|---|---|
| 双路径一致 | 实时播放与离线导出共用同一内核 | `HyperSoundEngine.process` 是唯一处理入口，worklet/script/离线三处都调它 |
| 确定性 | 同输入 + 同参数 ⇒ 同输出（位级） | 禁止 `Math.random`/`Date`/`console`；系数全部显式初始化 |
| 可测试 | 无浏览器依赖即可完整单测 | AudioNode 全部鸭子类型（stub）；DSP 模块纯函数式接口 |
| 零分配稳态 | 实时线程无 GC 抖动 | `process()` 内零分配，工作缓冲惰性扩容 |
| 参数安全 | 恶意/越界参数不产生 NaN/爆音 | 所有频率/增益/Q clamp；白名单解码；NaN/Infinity 拒绝 |
| 许可合规 | 不碰 GPL；LGPL 只链接不改 | soundtouchjs 以"不修改源码 + 动态 import 调用"使用 |

**关键架构决策（ADR 摘要）**：

1. **纯 TS 内核而非 Web Audio 图**：节点图在 128 帧量子下跨节点开销大、离线/实时难对齐；
   纯内核双路径天然一致，也方便把引擎塞进 AudioWorklet 单文件。
2. **参数快照语义**：`setParams` 每次收完整快照并整体替换，模块 setParams 即算系数、即时生效；
   不做增量 diff，避免状态分叉。
3. **设备档案不引入**：不依赖真实设备测量档案；"机型频响补偿"由
   **LoudnessComp auto 按音量通用曲线**承担（音量越低 → 低频 0-12dB / 高频 0-6dB 提升）。
4. **LGPL 链接合规**：soundtouchjs(LGPL-2.1) 原包副本随 `vendor/` 分发（含 LICENSE），
   适配层 `StretchLgplAdapter` 只调公开 API，不修改源码；动态 import 按需加载。

---

## 2. 总体架构

```
┌────────────────────────────── 第 5 层：公共出口 index.ts ──────────────────────────────┐
│ 类型 + HyperSoundEngine + 场景 + 分享串 + 分析 + 分离 + 全部 dsp 模块                    │
└───────────────────────────────────────┬────────────────────────────────────────────────┘
                                         │
┌───────────────────────────────────────┴────────────────────────────────────────────────┐
│ 第 4 层：集成与周边                                                                    │
│  browser.ts / integration/HyperSoundEngineHost.ts  音频图接线（worklet/script 双模式）  │
│  worklet/AudioEffectsProcessor.ts  AudioWorklet 处理器（打包单文件）                    │
│  analysis/Spectrum.ts、HearingTest.ts   频谱分析、听力测试                              │
│  offline/Separator.ts   声源分离任务队列（spleeter/demucs 适配，ONNX 占位）             │
└───────────────────────────────────────┬────────────────────────────────────────────────┘
                                         │ 调用 HyperSoundEngine.process / setParams / getStats
┌───────────────────────────────────────┴────────────────────────────────────────────────┐
│ 第 3 层：引擎总成 engine/                                                                │
│  HyperSoundEngine.ts    21 级处理链编排（唯一处理入口，零分配、确定性、latency、分析）    │
│  ScenePresets.ts  12 个组合场景预设（全参数快照）                                        │
│  ShareCodec.ts    分享串：base64url("<version>:<fnv1a32>:<json>") 白名单解码             │
└───────────────────────────────────────┬────────────────────────────────────────────────┘
                                         │ 调用 dsp 模块
┌───────────────────────────────────────┴────────────────────────────────────────────────┐
│ 第 2 层：DSP 内核 dsp/（16 个模块 + 1 个 LGPL 适配层，零运行时依赖）                    │
│  fft / biquad / EqChain / MidSide / Deesser / Compressor / Limiter / BassEnhancer       │
│  Convolver / ReverbSimple / LufsMeter / LoudnessComp / Resampler / Stretch / PitchYin   │
│  features（＋ StretchLgplAdapter 适配层 + soundtouchjs.d.ts 类型声明）                  │
└───────────────────────────────────────┬────────────────────────────────────────────────┘
                                         │ 参数类型（import type）
┌───────────────────────────────────────┴────────────────────────────────────────────────┐
│ 第 1 层：参数模型 types.ts  HyperSoundEngineParams 全量快照 + 默认值 createDefaultParams()│
└─────────────────────────────────────────────────────────────────────────────────────────┘
```

**分层规则**：第 2 层只 import 第 1 层的类型；第 3 层可调 2/1；第 4 层可调 3/2/1；
同层之间尽量不互相依赖（DSP 模块彼此独立，唯一例外是 EqChain/biquad 的公共 Biquad 类）。
依赖方向单向向下，无环。

---

## 3. 目录结构与模块清单

```
HyperSoundEngine/
├── src/
│   ├── index.ts                  # ★ 核心公共出口（纯 DSP，无浏览器/UI 依赖）
│   ├── browser.ts                # ★ 浏览器宿主入口（HyperSoundEngineHost）
│   ├── worklet.ts                # ★ AudioWorklet 打包入口
│   ├── interfaces.ts             # 对外统一接口（AudioEngine / StereoProcessor / ProcessingStage）
│   ├── types.ts                  # 参数模型 + createDefaultParams（唯一类型源）
│   ├── dsp/                      # 纯 DSP（零运行时依赖，实时/离线共用）
│   │   ├── fft.ts biquad.ts EqChain.ts MidSide.ts
│   │   ├── Deesser.ts Compressor.ts Limiter.ts BassEnhancer.ts
│   │   ├── Convolver.ts ReverbSimple.ts LufsMeter.ts LoudnessComp.ts
│   │   ├── Resampler.ts Stretch.ts StretchLgplAdapter.ts PitchYin.ts features.ts
│   │   ├── soundtouchjs.d.ts     # LGPL 库类型声明
│   │   └── API_SPEC.md           # DSP 模块实现契约
│   ├── engine/                   # 引擎总成
│   │   ├── HyperSoundEngine.ts   # 21 级链 + IEQ/NightMode/3D 环绕/响度归一化内置
│   │   ├── factory.ts            # createEngine / createHyperSoundEngine 工厂
│   │   ├── ScenePresets.ts       # 11 场景
│   │   └── ShareCodec.ts         # 分享串编解码
│   ├── integration/HyperSoundEngineHost.ts   # 浏览器引擎切换接线
│   ├── worklet/AudioEffectsProcessor.ts      # AudioWorklet 处理器
│   ├── analysis/                 # Spectrum.ts、HearingTest.ts
│   ├── offline/Separator.ts      # 分离任务队列
│   └── ...
├── adapters/
│   └── waveforge/                # 外部宿主适配示例（WaveForge 接线，独立于核心）
│       ├── attachEngine.ts
│       └── README.md
├── ui/                           # 可选 React 调音室 UI（不参与核心构建）
│   ├── HyperSoundEngineMixingStudio.tsx
│   ├── bridge.ts                 # HyperSoundEngineUiBridge（UI 与引擎的唯一接缝）
│   ├── theme.ts / primitives.tsx / hooks.ts / eqCurveEditor.tsx
│   ├── effectsPanel.tsx / eqPanel.tsx / sharePanel.tsx / analysisPanel.tsx
│   └── modalsSpatial.tsx / modalsDynamics.tsx / modalsLoudness.tsx
├── examples/                     # 独立接入示例（Node / Browser）
├── vendor/soundtouchjs/          # ★ LGPL-2.1 原包副本（含 LICENSE，未修改）
├── test/ + ui/uiSmoke.test.tsx   # 32 个 vitest 文件 / 331 用例
├── docs/                         # API / 架构 / 接入指南 / 功能核验 / 统计 / 审计
├── 架构书.md                     # 本文档
├── THIRD_PARTY_NOTICES.md
├── scripts/                      # 构建脚本（build-core / build-worklet）
└── package.json  tsconfig*.json
```

### DSP 模块一览（16 模块 + 适配层）

| 模块 | 文件 | 算法出处 | 关键边界处理 |
|---|---|---|---|
| FFT/窗 | `fft.ts` | kissfft 蝶形（BSD-3） | 长度必须 2 的幂，否则抛错 |
| 双二阶 | `biquad.ts` | RBJ Cookbook + DSPFilters TDF2（MIT） | f0 clamp [1, fs·0.45]；q>0；BLT 预畸变 |
| 参数 EQ | `EqChain.ts` | RBJ + 自研级联 Q 补偿 | 10/20 段；补偿后控制点误差 <0.02dB |
| M/S | `MidSide.ts` | 公有 2×2 矩阵 | 对称 vb 公式；width=0 退单声道 |
| 齿音抑制 | `Deesser.ts` | Stanford EE264 论文思路 | 侧链带通 + LR-4 交叉；分带/宽带 |
| 压缩器 | `Compressor.ts` | DaisySP（MIT）接口思路 | 软拐点；1/4 斜率 ±0.1 |
| 限幅器 | `Limiter.ts` | DaisySP + 自研 | 前瞻 + 4× 过采样真峰值；零过冲 |
| 虚拟低音 | `BassEnhancer.ts` | IEEE 虚拟低音论文 | 4 种谐波非线性；even→120Hz |
| 卷积混响 | `Convolver.ts` | DAFX 分区卷积论文 | 滑动窗口 + 动态扩容；IR 去周期化；块长 128–4096 对拍 |
| 算法混响 | `ReverbSimple.ts` | Freeverb（stk MIT） | 5 类型；能量单调衰减 |
| 响度计 | `LufsMeter.ts` | ITU-R BS.1770/EBU R128 | K 加权 + 门限；LRA/真峰值 |
| 等响度补偿 | `LoudnessComp.ts` | ISO 226 简化 | **auto 按音量通用曲线**；f0 clamp |
| 重采样 | `Resampler.ts` | speexdsp 多相（BSD-3） | 多相 Kaiser-sinc；流式=一次性 |
| 变速/变调 | `Stretch.ts` | signalsmith 思路（MIT） | WOLA 归一化阈值 0.01；参数突变 reset |
| 音高检测 | `PitchYin.ts` | YIN (2002) | 差分→CMND→阈值；噪声→-1 |
| 频谱特征 | `features.ts` | meyda 定义（MIT） | 质心/滚降/平坦度/波峰因子/ZCR |
| LGPL 适配 | `StretchLgplAdapter.ts` | soundtouchjs（LGPL-2.1 未修改链接） | 动态 import；位级确定 |

---

## 4. 处理链（HyperSoundEngine.process，21 级）

顺序**固定**（API_SPEC 辅助模块 A 亦登记）：

```
输入
 ├─ 1) 响度归一化   （实时 BS.1770 测量驱动或外部增益；3s 平滑；无测量期 0dB）
 ├─ 2) 3D 环绕      （轻量立体声旋转：angle 静态 + speed 慢旋；distance→电平）
 ├─ 3) M/S          （width + voiceBalance；voiceBalance 仅 pitch.enabled 时生效）
 ├─ 4) Pre-EQ       （用户 EQ：simple 5 段 / pro 10-20 段 + 级联 Q 补偿；eq.enabled 门控）
 ├─ 5) Deesser      （分带/宽带齿音抑制）
 ├─ 6) Compressor   （软拐点动态压缩）
 ├─ 7) NightMode    （压缩增强 + 6kHz 高频衰减）
 ├─ 8) Delay        （调制类延迟效果）
 ├─ 9) Chorus       （合唱）
 ├─ 10) Flanger     （镶边）
 ├─ 11) Phaser      （移相 2/4/6/8 级全通）
 ├─ 12) Tremolo     （颤音）
 ├─ 13) 混响        （四路路由：卷积（非均匀分区）| 算法 Freeverb | FDN 网络 | off；off=完全直通）
 ├─ 14) BassEnhancer（虚拟低频谐波）
 ├─ 15) LoudnessComp（等响度补偿：auto/preset/custom；auto=按音量通用曲线）
 ├─ 16) IEQ(Post)   （智能均衡：flat/warm/bright/vocal 目标曲线，3s 慢速平滑防抽吸）
 ├─ 17) [FFT 取样点]（频谱分析与特征提取）
 ├─ 18) DynamicEq   （自适应动态均衡：全通交叉分带 + 频谱包络自动混音）
 ├─ 19) [LUFS 取样点]（Limiter 之前，测压限前节目响度）
 ├─ 20) 调制主增益   （LFO/Envelope/MIDI 驱动的 masterGain）
 ├─ 21) Limiter     （前瞻 + 真峰值，默认 -1dBFS）
 └─ 输出
```

**链设计理由**：
- **响度归一化在最前**：统一输入电平，后续压缩/限制的阈值语义稳定；
- **Pre-EQ 在动态处理之前**：EQ 造成的峰值变化由压缩器/限幅器兜底；
- **LoudnessComp 在 IEQ 之前、限幅之前**：补偿产生的提升先被 IEQ 整形、被限幅器兜底，避免削波；
- **LUFS 取样点在 Limiter 之前**：测的是节目响度而非限幅后的峰值限制结果（API_SPEC 要求）；
- **IEQ 的分析取样在 LoudnessComp 之后**（即 IEQ 输入处），闭环反馈无延迟感。

**旁路语义**：每个效果模块 `enabled=false` 时逐样本直通（审计断言 diff=0）；
全关时输出与输入逐样本一致（≤1e-12 浮点容差）。

**分析路径**：`getAnalysis()` 在链内取样（2048 点 FFT，位于 LoudnessComp 之后），
输出频谱 + 特征（rms/质心/滚降/平坦度/波峰因子），供 UI 可视化与 IEQ 输入。

**latency**：由卷积混响分区长 + 限幅器 lookahead 决定，`getLatencySamples()` 精确报告；
混响 mode='off' 或算法混响时卷积链路直通、不贡献延迟。

---

## 5. 参数模型（types.ts）

- **`HyperSoundEngineParams`**：全量参数快照，包含 eq / deesser / compressor / nightMode / bassEnhancer /
  reverb / surround3d / loudnessCompensation / loudnessNormalization / limiter / ieq / pitch /
  stereoWidth 等子设置。
- **`createDefaultParams(fs)`**：唯一默认值入口；所有数值可序列化（JSON 安全）。
- **快照语义**：`setParams` 整包替换 + 各模块系数重算；分享串/场景预设均以快照为交换单位。
- **`deviceProfile` 不引入**：机型补偿由 `loudnessCompensation.mode='auto'` 承担。

**LoudnessComp auto —— 按音量实施的通用补偿曲线**：

```
spl   = 50 + 30 × (volumePercent / 100)          // 音量 0-100 → 感知电平 50-80 dB SPL
lowG  = clamp((80 − spl) × 0.35, 0, 12)  dB      // 低频提升上限 12dB @ 120Hz LowShelf(Q=0.707)
highG = clamp((80 − spl) × 0.15, 0, 6)   dB      // 高频提升上限 6dB  @ 12kHz HighShelf(Q=0.707)
```

音量越低 → 低/高频提升越多；音量 100% 时补偿为 0（曲线平直）。

---

## 6. 双路径执行模型

```
                 ┌─────────────────────────────────────────────┐
 用户操作/参数 → │ HyperSoundEngineHost（浏览器侧接线）           │
                 │  worklet 模式：AudioWorklet 内 HyperSoundEngine│
                 │  script 模式：ScriptProcessor 内 HyperSoundEngine│
                 └──────────────┬──────────────────────────────┘
                                │ 同一内核、同一参数
                 ┌──────────────┴──────────────────────────────┐
 离线导出 →       │ HyperSoundEngine.process 分块处理（解码 PCM → 链 → WAV）│
                 └─────────────────────────────────────────────┘
 一致性保证：worklet 与离线导出对同一输入+同一参数逐样本误差 <1e-6（测试断言）
```

**worklet 模式**：`AudioEffectsProcessor.ts` 经 esbuild 打包为单文件 `dist/worklet-bundle.js`，
处理器名 `hypersoundengine`；参数经 `port.postMessage({type:'params'})` 下发，`stats` 周期回传。

**script 兜底**：`createScriptProcessor(4096)`，`onaudioprocess` 内直接调 `HyperSoundEngine.process`
（同一内核，无需打包即可先联调）；块长默认 4096。

**离线路径**：解码后的 PCM 按块喂给同一个 HyperSoundEngine 实例，写出 WAV；可与实时链逐样本对拍。

---

## 7. 引擎总成细节（HyperSoundEngine）

- **零分配**：`process()` 内不 new 任何对象；工作缓冲按需惰性扩容，稳态无分配。
- **确定性**：无随机/Date/console；同输入同参数 ⇒ 位级相同输出。
- **单/双声道**：`new HyperSoundEngine(fs, channels)`；单声道忽略第二输入。
- **热切换安全**：`setParams` 中途不会撕裂处理（块间生效）；参数突变由模块内部平滑/reset 兜底
  （Stretch 突变 reset、Limiter lookahead 重建保留增益、响度归一化外部增益一阶平滑）。
- **内置实现**：IEQ（目标曲线拟合）、NightMode、3D 环绕、响度归一化控制都在本文件内，
  不再拆 dsp 模块（避免过度抽象）。

---

## 8. 集成层（HyperSoundEngineHost）

公开 API：

```ts
new HyperSoundEngineHost(opts?: {
  mode?: 'worklet'|'script'|'auto'
  workletUrl?: string
  processorName?: string
  blockSize?: number
  engine?: AudioEngine
  engineFactory?: (sampleRate: number, channelCount?: number) => AudioEngine
})
host.attach(handle: { audioContext; masterGain; analyser }, params?)  // 幂等
host.setParams(p: HyperSoundEngineParams)
host.dispose()        // 恢复 masterGain→analyser 直连
host.engine           // 引擎实例（惰性创建/注入）
```

**切换语义**：
1. attach：masterGain 全断 → 接入处理节点 → 连 analyser；
2. dispose：断开处理节点 → 恢复 masterGain→analyser 直连；
3. 幂等：重复 attach 同一 handle 直接 return；
4. 竞态：attach 异步注册期间被 dispose → 完成后放弃接线（防旧节点插进新图）。

AudioNode 均为鸭子类型（最小接口），Node 测试环境可用 stub 验证接线语义（`integration.test.ts` 9 项）。

---

## 9. 分享串与场景预设

**ShareCodec**（`engine/ShareCodec.ts`，自研）：
- 格式：`base64url("<version>:<checksum>:<json>")`，version 恒为 1（`SHARE_CODEC_VERSION`）；
- 校验：FNV-1a 32 位哈希（公有领域算法）覆盖 `version:json`，8 位小写十六进制；
- 安全：白名单解码（非法枚举回落默认、越界数值 clamp、长字符串截断、NaN/Infinity 拒绝）；
- 卷积 IR 数组序列化时降级为 `irName` 引用（避免超大串）。

**ScenePresets**（`engine/ScenePresets.ts`，自研）：
- 12 个组合场景（id 固定、顺序稳定），每个是全参数快照（含混响/补偿/EQ 等全部字段）；
- 场景切换 = setParams(快照)，A→B→A 热切换无爆音（审计断言）；
- 夜间/低频等补偿场景经四轮调平收敛到 -22.4dB 契约界内。

---

## 10. 第三方库与许可合规

| 类别 | 库 | 许可 | 使用方式 |
|---|---|---|---|
| 直接依赖（可选） | soundtouchjs v0.3.0 | **LGPL-2.1** | **不修改源码 + 动态 import 链接调用**；vendor/ 原包副本随附 LICENSE；适配层只调公开 API |
| 直接依赖（可选） | meyda ^5.6.0 | MIT | 频谱特征（未安装时自研特征照常） |
| 直接依赖（可选） | signalsmith-stretch ^1.0.0 | MIT | 变速/变调 WASM（纯 JS 环境回退自研） |
| 概念移植（自研实现） | DSPFilters / kissfft / stk / DaisySP / signalsmith-basics / speexdsp / meyda | MIT/BSD | 仅借鉴算法与公式，代码自研并头注释出处 |
| 公开标准 | RBJ Cookbook / ITU-R BS.1770 / ISO 226 / YIN(2002) 等 | 公开 | 公式实现 |
| **GPL/AGPL** | — | — | **零引入**（pitchfinder GPLv3 已回避） |

合规要点：LGPL 库以"不修改源码 + 动态/静态链接调用"方式使用；
`THIRD_PARTY_NOTICES.md` 随分发物。

---

## 11. 测试体系（32 文件 / 331 用例，全绿；tsc 0 错误）

| 层级 | 文件 | 用例 | 覆盖 |
|---|---|---|---|
| DSP 单测 | 16 个模块各自 test | ~180 | 算法正确性（频响/能量/往返对拍）+ 边界（NaN/clamp/零输入衰减） |
| 引擎测试 | engine.test.ts 等 | 12+ | 链直通、旁路、latency、双路径一致性 |
| 场景/分享串 | scenes / codec | 6+12 | 11 场景合法性；分享串往返 + 恶意输入注入防护 |
| 集成 | integration.test.ts | 9 | 切换幂等/竞态/dispose 恢复直连/script 通路限幅 |
| 公共 API | public-api.test.ts | 4 | 核心入口纯净、浏览器入口导出、工厂可用 |
| 卷积块长 | convolver-blocklen.test.ts | 4 | B=128/512/1024/4096 对拍双精度直接卷积（maxErr ≤ 3.4e-6） |
| 审计 | audit-chain(15) / audit-dsp(82) / audit-combo(27) | 124 | 全链健康、DSP 深检、组合边界 |

**审计历史**：3 个并行审计子代理 + 主代理完成全链路排查（`docs/audit/`），
发现并修复 12 类问题（含 Convolver 流式 NaN、分区丢失、LoudnessComp 8k NaN、MidSide 仅伴奏失效、
Stretch 突变炸音、响度启动膨胀等），修复后断言全部转正。

---

## 12. 已知边界与备查项

1. **卷积混响流式"末块尾截断"**：最后未满块的湿输出需后续输入冲刷（流式固有语义，文档已注）；
2. **3D 环绕级**未收录于 API_SPEC 模块 A（实现为链内轻量立体声旋转，差异已注释）；
3. **Limiter lookahead 变化**重建缓冲时 gain 复位台阶（热切换边界跳变 0.33 @0.5 幅度，有界）；
4. **部分场景 ~1e-3（-60dBFS）级微小 DC 残余**（偶次谐波整流+压缩包络调制，无累积、低于可闻阈值）；
5. **script 兜底块长默认 4096**：块长相关缺陷已修复（Convolver 滑动窗口+动态扩容），安全；
6. **声源分离**为任务队列 + 适配层，模型权重按 spleeter/demucs 各自仓库声明（ONNX 占位）。

---

## 13. 文档索引

| 文档 | 内容 |
|---|---|
| `docs/API.md` | 对外接口文档 |
| `docs/ARCHITECTURE.md` | 简要架构说明 |
| `docs/INTEGRATION.md` | 接入其他软件指南 |
| `docs/FEATURES_VERIFICATION.md` | 功能核验报告（26 项有效功能 / MIT·LGPL 统计） |
| `docs/代码构成与自研统计.md` | 自研/现成统计 |
| `docs/audit/SUMMARY.md` + chain/dsp/combo/final-acceptance | 审计与修复记录 |
| `src/dsp/API_SPEC.md` | DSP 模块实现契约 |
| `THIRD_PARTY_NOTICES.md` | 第三方许可声明 |
| `adapters/waveforge/README.md` | 外部宿主（WaveForge）适配说明 |
| `research/docs/音频算法设计文档.md` | 设计文档 |
| `research/docs/音频算法技术文档.md` | 算法技术文档 |
