# hse-napi（占位目录，crate 未建）

**一句话职责**（源自《原生化双支线与Windows音频接入规划书》§2.1）：基于 napi-rs 的 Node/Electron 进程内嵌入扩展，把 Rust 引擎以原生模块形式暴露给既有 JS 接入方的可选路径。

**启动节奏：Phase 2+（按需启动）**。当前目录仅含本说明文档，没有 Cargo 清单；启动时再创建 crate 并加入 workspace 的 `members`。
